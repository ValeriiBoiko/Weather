import { StyleSheet } from "react-native";

export const common = StyleSheet.create({
    flex: {
        flex: 1
    },
    row: {
        flexDirection: 'row',
        alignItems: 'center'
    }
}) 